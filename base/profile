PATH=$HOME/bin:$PATH

# Silence macOS pre-release warning
export HOMEBREW_DEVELOPER=1
[[ ! -f /opt/homebrew/bin/brew ]] || eval "$(/opt/homebrew/bin/brew shellenv)"

[[ ! -f ~/.profile.local ]] || . ~/.profile.local
